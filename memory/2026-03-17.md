# 2026-03-17 - 备婚大作战部署调试

## 早间问题
- Git push 到 GitHub 失败（网络连接问题）
  - 错误1: `Connection reset by peer`
  - 错误2: `Failed to connect to github.com port 443`
- Vercel 部署后访问超时（PostgreSQL 连接问题）

## 修复内容
1. 添加 `index.py` 入口文件解决 Vercel entrypoint 错误
2. 更新 `vercel.json` 指向 `index.py`
3. 完整 PostgreSQL 支持：
   - `DBAdapter` 类自动转换 `?` → `%s`
   - `RealDictCursor` 统一字段名访问
   - `@app.before_first_request` 自动初始化数据库
4. 修复 `auth.py` 统一 COUNT(*) 查询别名
5. `VERCEL_DEPLOY.md` 新增详细故障排除指南

## 待解决
- [ ] GitHub 网络连接恢复后重新推送
- [ ] Vercel 重新部署并测试访问
- [ ] 验证 Neon PostgreSQL 连接和表初始化
- [ ] 测试登录功能是否正常

## 下一步
1. 等待网络恢复或使用代理
2. 推送最新代码到 GitHub
3. Vercel 重新构建
4. 检查环境变量 `DATABASE_URL` 格式
5. 查看 Vercel Logs 确认数据库连接